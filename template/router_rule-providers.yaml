rule-providers:
  Loyalsoldier-reject: { type: http, behavior: domain, url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/reject.txt", path: ./ruleset/Loyalsoldier-reject.yaml, interval: 86400 }
  Loyalsoldier-icloud: { type: http, behavior: domain, url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/icloud.txt", path: ./ruleset/Loyalsoldier-icloud.yaml, interval: 86400 }
  Loyalsoldier-apple: { type: http, behavior: domain, url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/apple.txt", path: ./ruleset/Loyalsoldier-apple.yaml, interval: 86400 }
  Loyalsoldier-google: { type: http, behavior: domain, url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/google.txt", path: ./ruleset/Loyalsoldier-google.yaml, interval: 86400 }
  Loyalsoldier-proxy: { type: http, behavior: domain, url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/proxy.txt", path: ./ruleset/Loyalsoldier-proxy.yaml, interval: 86400 }
  Loyalsoldier-direct: { type: http, behavior: domain, url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/direct.txt", path: ./ruleset/Loyalsoldier-direct.yaml, interval: 86400 }
  Loyalsoldier-private: { type: http, behavior: domain, url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/private.txt", path: ./ruleset/Loyalsoldier-private.yaml, interval: 86400 }
  Loyalsoldier-gfw: { type: http, behavior: domain, url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/gfw.txt", path: ./ruleset/Loyalsoldier-gfw.yaml, interval: 86400 }
  Loyalsoldier-tld-not-cn: { type: http, behavior: domain, url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/tld-not-cn.txt", path: ./ruleset/Loyalsoldier-tld-not-cn.yaml, interval: 86400 }
  Loyalsoldier-telegramcidr: { type: http, behavior: ipcidr, url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/telegramcidr.txt", path: ./ruleset/Loyalsoldier-telegramcidr.yaml, interval: 86400 }
  Loyalsoldier-cncidr: { type: http, behavior: ipcidr, url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/cncidr.txt", path: ./ruleset/Loyalsoldier-cncidr.yaml, interval: 86400 }
  Loyalsoldier-lancidr: { type: http, behavior: ipcidr, url: "https://cdn.jsdelivr.net/gh/Loyalsoldier/clash-rules@release/lancidr.txt", path: ./ruleset/Loyalsoldier-lancidr.yaml, interval: 86400 }